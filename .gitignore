# Build output
bin/
obj/
*.dacpac

# Publish profiles (may contain connection strings)
*.publish.xml

# Fabric export packaging - the definition is kept, the thumbnail manifest is not
**/manifest.json

